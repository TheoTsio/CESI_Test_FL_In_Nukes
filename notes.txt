docker build -t flower-app:latest

If an error happens that says the docker daemon is deactivated or that cannot find a running docker you run:
sudo systemctl status docker 
and if it says deactivate you run 
sudo sustemctl start docker 
After that try to create the docker image again


docker run --rm   --name flower-server   --network flower-net   -p 8080:8080   flower-app:latest   python server_app.py

ssh -N -L 0.0.0.0:8080:127.0.0.1:8080 ncuser@10.191.44.101

sudo docker run --rm   --name flower-client-0   --add-host=host.docker.internal:host-gateway   flower-app:latest   python client_app.py 0 host.docker.internal

sudo ufw allow 8080/tcp

sudo pacman -Syu tailscale
sudo systemctl enable --now tailscaled
sudo tailscale up
tailscale ip -4


sudo docker run --rm \
  --name flower-client-1 \
  flower-app:latest \
  python client_app.py 0 100.115.119.86

sudo docker network rm flower-net
sudo docker network create --driver bridge -o "com.docker.network.driver.mtu=1280" flower-net
sudo docker run --rm --net=flower-net --name flower-client-2 flower-app:latest python client_app.py 0 100.115.119.86


sudo docker run --rm \
  -e GRPC_ENABLE_FORK_SUPPORT=false \
  --net=flower-net \
  --name flower-client-2 \
  flower-app:latest python client_app.py 0 100.115.119.86
